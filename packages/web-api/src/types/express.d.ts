import 'express';
import type { AuthSession } from '@agentx/shared';

declare module 'express' {
  interface Request {
    /** Short request ID generated by the request-id middleware. */
    requestId?: string;
    /** Authenticated session attached by syncDEKMiddleware. */
    agentxSession?: AuthSession;
  }
}
