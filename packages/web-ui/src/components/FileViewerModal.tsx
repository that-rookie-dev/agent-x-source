/**
 * App-wide file viewer (images, PDF, text/table preview).
 * Thin alias over the chat AttachmentModal so Knowledge Base and chat share one surface.
 */
export { AttachmentModal as FileViewerModal } from '../chat/AttachmentModal';
